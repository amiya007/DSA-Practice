#include <bits/stdc++.h>
using namespace std;

int noOfMaxMeeting(vector<int> start, vector<int> end)
{
    int meetCount = 0;
    vector<vector<int>> sortedMeeting;
    for (int i = 0; i < start.size(); i++)
    {
        
    }

    return meetCount;
}

int main()
{
    vector<int> starting = {0, 3, 1, 5, 5, 8};
    vector<int> ending = {5, 4, 2, 9, 7, 9};
    cout << noOfMaxMeeting(starting, ending);
    return 0;
}